import type { Config } from 'vike/types'

export default {
  title: 'Reserve your Stasher',
  description: 'Reserve a place in the queue for Stasher, the air-gapped hardware wallet. No payment now — reserving costs nothing.',
} satisfies Config
