import { AppShell } from '../AppShell.tsx'

// Vike route for /preorder — the unpaid reservation form (WEB-1/WEB-2 v1).
export default function Page() {
  return <AppShell ssrLocation="/preorder" />
}
