import type { Config } from 'vike/types'

export default {
  title: 'Stasher app — desktop companion',
  description: 'Download the free Stasher companion app for Windows and Linux. See every coin, build transactions, and approve each one on your Stasher X1 — your keys never leave the device.',
} satisfies Config
