/** Hover card shown under a nav link — a product teaser, not a menu. */
export type NavLinkPreview = {
  image: string;
  kicker: string;
  title: string;
  description: string;
  points: string[];
  cta: string;
};
