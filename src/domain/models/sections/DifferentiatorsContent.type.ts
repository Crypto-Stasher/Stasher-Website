export type DifferentiatorItem = {
  id: string;
  label: string;
  title: string;
  description: string;
};

export type DifferentiatorsContent = {
  title: string;
  items: DifferentiatorItem[];
};
